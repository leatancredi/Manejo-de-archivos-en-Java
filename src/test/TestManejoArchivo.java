package test;

import static manejoarchivos.ManejoArchivos.*;

public class TestManejoArchivo {
    public static void main(String[] args) {
        //definimos una variable para recibir el archivo
        var nombreArchivo = "prueba.txt";
        //llamamos al metodo estatico "crear archivo"
//        crearArchivo(nombreArchivo);
//    escribirArchivo(nombreArchivo, "hola desde java!");
//    anexarArchivo(nombreArchivo, "chau desde java!");
    leerArchivo(nombreArchivo);
    
    }
 
}
